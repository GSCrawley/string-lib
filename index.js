
String.prototype.allCaps = function() {
    return this.toUpperCase()
}

String.prototype.capitalizewords = function(){
    let str = this.split('')
    
    str = str.capitalize()
    while (i < s.len) {
        if
    }
}